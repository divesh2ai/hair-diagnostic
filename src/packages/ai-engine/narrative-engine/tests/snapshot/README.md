# Narrative Engine — Snapshot Directory

Jest snapshot files are auto-generated here when running `narrativePipeline.test.ts` with `.toMatchSnapshot()`.

To update snapshots after intentional output changes:
```
npx jest --updateSnapshot
```
