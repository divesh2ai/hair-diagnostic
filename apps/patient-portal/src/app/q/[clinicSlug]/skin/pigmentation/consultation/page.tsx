import Link from 'next/link';
export default async function Page({params,searchParams}:{params:Promise<{clinicSlug:string}>;searchParams:Promise<{assessmentId?:string}>}){const {clinicSlug}=await params;const {assessmentId}=await searchParams;return <main className="mx-auto max-w-xl px-6 py-24"><p className="text-xs uppercase tracking-widest text-amber-700">Dr Skin FACT · Pigmentation</p><h1 className="mt-4 font-serif text-5xl">Video consultation</h1><p className="mt-5 leading-7 text-stone-600">Your preference has been recorded. The clinical team will confirm a secure appointment time before final diagnosis and prescription.</p><p className="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900">Status: Scheduling pending{assessmentId?` · ${assessmentId}`:''}</p><Link className="mt-8 inline-block underline" href={`/q/${clinicSlug}/skin`}>Return to Skin FACT</Link></main>}
