{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm install && cd apps/patient-portal && npm install && npx prisma generate --schema ../../prisma/schema.prisma",
  "crons": [
    {
      "path": "/api/internal/report-assets/render",
      "schedule": "0 2 * * *"
    }
  ]
}
